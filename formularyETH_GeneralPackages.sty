\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{formularyETH_GeneralPackages}[2016/10/11 General not specific packages]
% ======================================================================
% Packages
% ======================================================================
% Displaying shortcut keys
\RequirePackage{menukeys}    
 % x/y fashion table headers
\RequirePackage{diagbox}
% allows \textlangle and \textrangle inside text
\RequirePackage{textcomp}
\RequirePackage{hyperref} 													% Clever references, also needed for \cref
\hypersetup{
    colorlinks,
    linkcolor={red!50!black},
    citecolor={blue!50!black},
    urlcolor={blue!80!black}
}
\RequirePackage{cleveref}
% ------------------------------------------------------------------------------
% import allows relative subsub imports
% ------------------------------------------------------------------------------
% \usepackage{import}
\usepackage{moresize}
% ------------------------------------------------------------------------------
% Fonts
% ------------------------------------------------------------------------------
% These two packages are needed for a centered tilde ~ inside minted envioments
\RequirePackage[T1]{fontenc}
\RequirePackage{lmodern}
% ------------------------------------------------------------------------------
% Figures
% ------------------------------------------------------------------------------
% For tikz resize
\RequirePackage{environ}
\usepackage{wrapfig}
\usepackage{cutwin}
% In order to use inkscape figures with transparent fillings using alpha
\usepackage{transparent}
% ------------------------------------------------------------------------------
% Tables
% ------------------------------------------------------------------------------
\usepackage{tabularx}
